{
  "name": "applyease-license-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "generate:codes": "node scripts/generate-codes.js",
    "codes:generate-paid": "node scripts/generate-paid-codes.js",
    "codes:issue": "node scripts/issue-unused-code.js"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
